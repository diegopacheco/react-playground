import React from "react";

const style = {
  background: "#00c",
  color: "#fff",
  padding: 12,
};

const Button = () => <button style={style}>App 2 Button(React 18)</button>;

export default Button;
